# 小龙虾对话提示词指南 — QStock 完整部署流程

> **用途**：按顺序将以下提示词逐条发送给 JVS Claw 小龙虾，完成 QStock 项目的平台部署、技能注册、功能测试和定时任务配置。
> **前置条件**：代码已上传 GitHub（https://github.com/qin-yincheng/QStock.git）
> **预计总耗时**：1-2 小时

---

## 阶段一：环境部署（3 条对话）

### 对话 1 — 克隆代码并安装依赖 （遇到无法安装pandas_ta,选择升级python版本）

```
请帮我在工作区部署一个量化分析项目。执行以下步骤：

1. 进入工作区目录：
2. 克隆我的 GitHub 仓库：
   git clone https://github.com/qin-yincheng/QStock.git
3. 进入项目目录并安装依赖：
   pip install -r requirements.txt
4. 验证安装是否成功：
   python -c "import czsc; print(f'czsc版本: {czsc.__version__}')"
   python -c "import pandas_ta; print('pandas_ta OK')"
   python -c "import tushare; print(f'tushare版本: {tushare.__version__}')"
如果 requirements.txt 中某些包安装失败（如 baostock），可以忽略，核心依赖是 czsc、tushare、pandas_ta 这三个。
请告诉我每一步的执行结果。
```
> **检查点**：三个核心包版本号正常输出即为成功。
龙虾输出
🔧 关键技术细节
Python 版本升级方案：

原系统 Python：3.10.12（不满足 pandas_ta 要求）
使用 pyenv 安装 Python 3.12.13
创建独立虚拟环境，避免污染系统 Python
项目路径：

项目目录：
/home/admin/openclaw/workspace/QStock
虚拟环境：
/home/admin/openclaw/workspace/QStock/venv
使用方式：

cd /home/admin/openclaw/workspace/QStock
source venv/bin/activate
python your_script.py
---

### 对话 2 — 配置 Tushare Token

> ⚠️ **重要**：将下面的 `这里替换为你的真实token` 替换为你的实际 Tushare Token。

```
请帮我配置 Tushare 数据源的 Token。执行以下 Python 代码：
进入项目目录
python -c "
import czsc
czsc.set_url_token(token='810cafd073d3dafa10a68b9fecfb61c291e65e63cfad68503d3b10cf', url='http://api.tushare.pro')
print('Token 设置完成')
"
然后验证 Token 是否可用：
python -c "
import tushare as ts
pro = ts.pro_api()
df = pro.daily(ts_code='600519.SH', start_date='20260301', end_date='20260325')
print(f'验证成功，获取到 {len(df)} 条日线数据')
print(df.head(3))
"
请告诉我 Token 配置和验证的结果。
```

> **检查点**：输出"验证成功，获取到 XX 条日线数据"即为成功。

---

### 对话 3 — 验证核心脚本可运行

```
请帮我验证 QStock 项目的核心脚本是否能正常运行。依次执行以下命令：
进入项目目录
# 测试1：单股分析（30分钟频率，分析宁德时代）
python scripts/analyze.py --symbol 300750.SZ
# 测试2：查看自选池
python scripts/portfolio.py --action list
如果两个脚本都能正常输出结果，说明环境部署成功。如果报错，请把完整的错误信息告诉我。
```

> **检查点**：analyze.py 输出信号分析结果（含缠论信号、双龙信号、融合判断），portfolio.py 输出 10 只默认股票列表。

---

## 阶段二：注册 4 个 Skill（1 条对话）

### 对话 4 — 注册技能

```
我的 QStock 项目中已经准备好了 4 个 Skill 文档，位于 skills/ 目录下。请帮我注册这 4 个技能：
1. qstock-scanner（🔍 股票池扫描选股）
   路径：/home/admin/openclaw/workspace/QStock/skills/qstock-scanner/SKILL.md
2. qstock-backtest（📊 单股回测验证）
   路径：/home/admin/openclaw/workspace/QStock/skills/qstock-backtest/SKILL.md
3. qstock-portfolio（📋 股票池管理）
   路径：/home/admin/openclaw/workspace/QStock/skills/qstock-portfolio/SKILL.md
4. qstock-advisor（🧠 缠论智能顾问）
   路径：/home/admin/openclaw/workspace/QStock/skills/qstock-advisor/SKILL.md
   参考资料目录：/home/admin/openclaw/workspace/QStock/skills/qstock-advisor/references/
请将这 4 个 Skill 注册到平台，使其可以被对话触发。如果需要将 skills 目录链接到平台识别的路径，请执行：
ln -s /home/admin/openclaw/workspace/QStock/skills/qstock-scanner /home/admin/openclaw/workspace/skills/qstock-scanner
ln -s /home/admin/openclaw/workspace/QStock/skills/qstock-backtest /home/admin/openclaw/workspace/skills/qstock-backtest
ln -s /home/admin/openclaw/workspace/QStock/skills/qstock-portfolio /home/admin/openclaw/workspace/skills/qstock-portfolio
ln -s /home/admin/openclaw/workspace/QStock/skills/qstock-advisor /home/admin/openclaw/workspace/skills/qstock-advisor
注册完成后请刷新技能列表，确认 4 个技能都已可用。
```

> **检查点**：小龙虾确认 4 个技能已注册并可触发。

---

## 阶段三：逐一测试 4 个 Skill（4 条对话）

### 对话 5 — 测试 Scanner 技能（扫描选股）

**第一轮：扫描自选池**

```
帮我扫描一下我的自选股票池，看看今天有什么信号。
```

> **预期**：调用 `python scripts/scan.py --portfolio`，输出推荐/关注/淘汰三个层级的结果。

**第二轮（追问）：指定板块扫描**

```
只看新能源板块有没有机会？帮我扫描一下宁德时代、比亚迪、隆基绿能。
```

> **预期**：调用 `python scripts/scan.py --symbols 300750.SZ,002594.SZ,601012.SH`。

---

### 对话 6 — 测试 Backtest 技能（回测验证）

**第一轮：双模式对比回测**

```
帮我回测一下宁德时代过去一年的表现，用融合模式和纯双龙模式都跑一下对比看看。
```

> **预期**：调用 `python scripts/backtest_report.py --symbol 300750.SZ --mode both`，输出收益率、胜率、盈亏比、最大回撤、夏普比率等指标 + 两种模式对比。

**第二轮（追问）：多频率对比**

```
用 60 分钟频率再回测一次宁德时代，我想对比 30 分钟和 60 分钟哪个效果好。
```

> **预期**：调用 `python scripts/backtest_report.py --symbol 300750.SZ --mode both --freq F60`。

---

### 对话 7 — 测试 Portfolio 技能（股票池管理）

**第一轮：查看自选池**

```
帮我看看我的自选池里有哪些股票。
```

> **预期**：调用 `python scripts/portfolio.py --action list`，展示 10 只默认股票。

**第二轮（追问）：增删操作**

```
把平安银行移出自选池，加入药明康德（603259.SH）。
```

> **预期**：依次调用 remove 和 add。

**第三轮（追问）：批量诊断**

```
对我的自选池做个全面诊断，看看每只股票现在什么信号。
```

> **预期**：调用 `python scripts/portfolio.py --action diagnose`。

---

### 对话 8 — 测试 Advisor 技能（缠论顾问）

**第一轮：缠论深度分析**

```
从缠论的角度帮我深度分析一下宁德时代目前的走势，包括笔结构、中枢位置、三类买卖点判断，以及接下来可能的三种演化路径。
```

> **预期**：调用 `python scripts/analyze.py --symbol 300750.SZ`，然后用缠论思维框架进行深度解读——包含"不测而测""完全分类""级别思维"三个维度的分析。

**第二轮（追问）：操作建议**

```
如果我现在没有持仓宁德时代，你建议在什么位置、什么条件下入场？止损设在哪？
```

> **预期**：基于缠论中枢位置给出进场价位、止损价位和目标价位的建议。

---

## 阶段四：配置定时任务（1 条对话）

### 对话 9 — 设置自动扫描

```
请帮我设置以下定时任务，在每个交易日自动运行股票扫描：

1. 每个交易日 10:31，扫描自选池（30分钟K线第2根收盘后）：
   cd /home/admin/openclaw/workspace/QStock && python scripts/scan.py --portfolio

2. 每个交易日 11:31，全量扫描50只股票池（上午收盘）：
   cd /home/admin/openclaw/workspace/QStock && python scripts/scan.py --output docs/scan_report_am.md

3. 每个交易日 15:01，收盘全量扫描 + 生成日报（收盘后）：
   cd /home/admin/openclaw/workspace/QStock && python scripts/scan.py --output docs/scan_report_daily.md

扫描完成后请自动通知我结果摘要（有信号的股票列表）。

另外，非交易日（周末和法定假日）不需要运行。
```

> **检查点**：小龙虾确认定时任务已设置，并说明触发方式和通知方式。

---

## 阶段五：端到端完整工作流测试（2 条对话）

### 对话 10 — 模拟完整日常工作流

```
现在模拟一个完整的盘后分析工作流：

1. 先扫描我的自选池，看看今天有什么信号
2. 对扫描出来信号最强的那只股票，做一个历史回测验证
3. 再用缠论框架深度分析这只股票的走势
4. 根据分析结果，告诉我是否应该将它保留在自选池中

请按步骤执行，每一步都给我详细的结果报告。
```

> **预期**：小龙虾依次调用 scanner → backtest → advisor → portfolio 四个技能，串联完成完整工作流。

---

### 对话 11 — 测试多频率对比分析

```
帮我对比分析宁德时代在 30 分钟和 60 分钟两个频率下的表现：

1. 先分别用 30 分钟和 60 分钟做回测对比
2. 再分别用两个频率分析当前信号
3. 综合两个频率的结果，给我一个最终的操作建议

我想看看不同频率下策略表现的差异。
```

> **预期**：分别用 --freq F30 和 --freq F60 跑回测和分析，给出对比总结。

---

## 阶段六：日常使用话术速查表

以下是部署完成后，你日常使用时可以随时对小龙虾说的话：

### 扫描选股

| 你说的话 | 触发技能 | 实际命令 |
|---------|---------|---------|
| 帮我看看今天市场有什么机会 | scanner | `scan.py` |
| 扫描一下我的自选池 | scanner | `scan.py --portfolio` |
| 新能源板块有哪些值得关注的 | scanner | `scan.py --symbols ...` |
| 用60分钟再扫一遍 | scanner | `scan.py --freq F60` |
| 帮我扫描并生成报告 | scanner | `scan.py --output docs/scan_report.md` |

### 回测验证

| 你说的话 | 触发技能 | 实际命令 |
|---------|---------|---------|
| 帮我回测一下比亚迪 | backtest | `backtest_report.py --symbol 002594.SZ` |
| 宁德时代过去半年表现怎样 | backtest | `backtest_report.py --symbol 300750.SZ --sdt ...` |
| 30分钟和60分钟对比回测茅台 | backtest | 分别 `--freq F30` 和 `--freq F60` |
| 融合模式和纯双龙模式哪个好 | backtest | `backtest_report.py --mode both` |

### 股票池管理

| 你说的话 | 触发技能 | 实际命令 |
|---------|---------|---------|
| 看看我的自选池 | portfolio | `portfolio.py --action list` |
| 把XX加入自选 | portfolio | `portfolio.py --action add --symbol XX` |
| 移除XX | portfolio | `portfolio.py --action remove --symbol XX` |
| 对自选池做个诊断 | portfolio | `portfolio.py --action diagnose` |
| 对自选池全部回测 | portfolio | `portfolio.py --action backtest-all` |

### 持仓管理

| 你说的话 | 触发技能 | 实际命令 |
|---------|---------|---------|
| 我买了500股宁德时代，195.5元 | holding | `portfolio.py --action update-holding --symbol 300750.SZ --buy-price 195.5 --shares 500` |
| 宁德时代卖了，210块 | holding | `portfolio.py --action clear-holding --symbol 300750.SZ --sell-price 210` |
| 我现在持有什么 | holding | `portfolio.py --action holdings` |
| 比亚迪302.8买的 | holding | `portfolio.py --action update-holding --symbol 002594.SZ --buy-price 302.8` |
| 清仓茅台 | holding | `portfolio.py --action clear-holding --symbol 600519.SH` |

### 深度分析

| 你说的话 | 触发技能 | 实际命令 |
|---------|---------|---------|
| 从缠论角度分析XX的走势 | advisor | `analyze.py --symbol XX` |
| 中枢在什么位置 | advisor | `analyze.py --symbol XX` + 缠论推理 |
| 接下来可能怎么走 | advisor | 完全分类三条路径分析 |
| 这个买入信号为什么可信 | advisor | 解释融合逻辑 |
| 从多个级别帮我看看XX | advisor | 分别 `--freq F60/F30/F15` |

---

## 常见问题处理

### 问题1：小龙虾找不到脚本 / 路径错误

```
QStock 项目的根目录在 /home/admin/openclaw/workspace/QStock，所有脚本执行前请先 cd 到这个目录。核心脚本路径：
- 扫描选股：python scripts/scan.py
- 单股分析：python scripts/analyze.py --symbol {代码}
- 回测报告：python scripts/backtest_report.py --symbol {代码}
- 股票池管理：python scripts/portfolio.py --action {操作}
```

### 问题2：Tushare 报限频错误

```
Tushare 接口有频率限制，请在连续请求之间加入 1-2 秒的间隔。如果扫描 50 只股票超时，可以先用 --portfolio 只扫描自选池（10只），或用 --symbols 指定少量股票。
```

### 问题3：某个 Python 包报 ImportError

```
请在 QStock 项目目录下重新安装依赖：
cd /home/admin/openclaw/workspace/QStock
pip install -r requirements.txt

如果单个包安装失败，请单独安装：
pip install czsc -U
pip install tushare -U
pip install pandas_ta
```

### 问题4：需要更新代码（本地改了代码后）

```
请帮我更新 QStock 项目代码：
cd /home/admin/openclaw/workspace/QStock
git pull origin main
```

### 问题5：平台实际路径与文档不一致

> 如果小龙虾的工作区目录不是 `/home/admin/openclaw/workspace/`，请在对话 1 执行后根据实际路径修改后续所有命令中的路径。
>
> 确认实际路径的方法：
> ```
> 请告诉我当前的工作区目录路径是什么？执行 pwd 看一下。
> ```

---

## 完成标志

当以下所有条件都满足时，Phase 3 + Phase 4 完成：

- [ ] 代码成功克隆到小龙虾平台
- [ ] czsc / tushare / pandas_ta 安装成功
- [ ] Tushare Token 配置并验证通过
- [ ] 4 个核心脚本可正常运行（analyze / scan / backtest_report / portfolio）
- [ ] 4 个 Skill 注册成功（scanner / backtest / portfolio / advisor）
- [ ] Scanner 技能：扫描自选池 + 指定股票均正常
- [ ] Backtest 技能：双模式对比 + 多频率回测正常
- [ ] Portfolio 技能：增删改查 + 批量诊断正常
- [ ] Advisor 技能：缠论深度分析 + 操作建议正常
- [ ] 定时任务已配置（10:31 / 11:31 / 15:01）
- [ ] 端到端完整工作流测试通过

---

*创建日期：2026-03-25*
*对应阶段：第十阶段 Phase 3 + Phase 4*
