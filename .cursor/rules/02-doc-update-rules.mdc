---
description: 文档更新规则 — 何时必须更新哪些文档
alwaysApply: true
---

# 文档更新规则

## 必须更新的触发条件

### 触发更新 `docs/已完成工作.md`

以下任意一项完成后，**必须**在文件头部（倒序）追加记录：

- 完成 `可执行步骤.md` 中的任意一个 Step（1.x / 2.x / 3.x ...）
- 发现并修复了 czsc 或其他库的 bug
- 验证测试脚本通过（附上输出结果摘要）
- 确定了重要技术决策（如数据方案选择）
- 创建了新的核心模块文件

**记录格式**：
```markdown
## YYYY-MM-DD

### Step X.X — 简短标题 ✅

- 做了什么（操作描述）
- 关键输出/结果（数据量、测试结果等）
- 遇到的问题和解决方案（如有）
- 产出物：文件名或结论
```

### 触发更新 `docs/当前阶段.md`

以下情况必须更新阶段状态表：

- 某个 Step 状态从「未开始」变为「进行中」→ 改为 `🔄 进行中`
- 某个 Step 完成 → 改为 `✅ 已完成`
- 某个 Step 决定跳过 → 改为 `⏭️ 跳过`，并注明原因
- 整个阶段完成，进入下一阶段 → 更新「当前阶段」标题和「下一步行动」
- 文件末尾「最后更新」时间必须同步修改

### 触发更新 `可执行步骤.md`

**不要随意修改此文件**，仅在以下情况更新：

- 发现步骤中的代码有错误或已过时 → 在对应步骤下加 `> ⚠️ 更新 YYYY-MM-DD：...` 注释
- 新增了原文档中未覆盖的步骤 → 在对应阶段末尾追加

## 更新顺序要求

完成一个 Step 后，按以下顺序更新文档：

1. 先更新 `docs/已完成工作.md`（记录细节）
2. 再更新 `docs/当前阶段.md`（更新状态表和下一步行动）
3. 如有必要，更新 `可执行步骤.md`

## 不需要更新文档的情况

- 调试过程中的中间状态（只有最终结果才记录）
- 代码小改动（不影响功能的重构/格式化）
- 阅读/学习类操作（克隆参考仓库除外）
